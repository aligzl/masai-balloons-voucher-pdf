{
  "name": "masai-balloons-voucher-pdf",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone Cloudflare Worker that renders the Masai Balloons flight voucher PDF (pdf-lib + fontkit), kept separate from the main app to stay under the Worker size limit.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
